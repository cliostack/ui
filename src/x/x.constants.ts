export const XConstants = {
    Environments: ['dom'] as const,
    DOM: {
        Spaces: ['ui'] as const,
        Categories: ['layout'] as const,
        Modules: ['vstack', 'hstack', 'view'] as const
    }
}
